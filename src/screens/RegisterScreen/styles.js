import { StyleSheet } from 'react-native';
import { marginSize } from '../../styles/sizes';

const styles = StyleSheet.create({
    input: {
        marginBottom: marginSize.small
    }
});

export default styles;